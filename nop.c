#include "monty.h"

/**
 * nop - A function that does nothing when executed.
 * @stack: The stack.
 * @line_number: Line number of the instruction.
 * Return: void
 */

void nop(stack_t **head, unsigned int line_number)
{
    (void) head;
	(void) line_number;
}